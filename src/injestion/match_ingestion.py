'''
This is where riot_api and postgre come together
'''


# match_ids = riot_client.get_ranked_match_ids()

# existing_ids = database.get_existing_match_ids()

# new_match_ids = ...

# for match_id in new_match_ids:
#     game_json = riot_client.get_match_data(match_id)
#     database.insert_match(match_id, game_json)